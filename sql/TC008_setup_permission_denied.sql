-- =============================================================================
-- TC008 異常系: DB 更新失敗の注入（権限エラー）
--   テスト用ログインから UPDATE 権限を剥奪した状態で batch を動かす。
--   ※ この SQL を流すログインには権限変更の権限が必要。
--     権限を触れない環境では TC007（制約違反）方式を使うこと。
-- =============================================================================
DENY UPDATE ON dbo.T_ORDER TO [batch_test_user];
GO
